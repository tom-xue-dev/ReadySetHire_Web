export default function JobApplicationPage({id}: {id: string}) {
    return (
        <section>
            <h2>Job Application Page{id}</h2>
            <p>This is a placeholder page for testing navigation.</p>
        </section>
    )
}