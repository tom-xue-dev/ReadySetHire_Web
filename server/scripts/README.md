# Scripts 使用说明

这个目录包含了项目的各种初始化和维护脚本。

## 可用脚本

### 1. 创建管理员用户

创建或更新管理员用户账号。

```bash
npm run db:create-admin [username] [email] [password]
```

**示例：**
```bash
# 使用默认参数（admin / admin@readysethire.com / admin123）
npm run db:create-admin

# 自定义参数
npm run db:create-admin myAdmin admin@company.com myPassword123
```

### 2. 初始化岗位数据

创建 20 个示例岗位，包含各种技术和业务岗位。

```bash
npm run db:init-jobs
```

**功能：**
- 自动创建或使用现有的 admin 用户
- 创建 20 个不同类型的岗位（前端、后端、全栈、AI、移动端等）
- 所有岗位默认状态为 PUBLISHED
- 如果已存在岗位，会询问是否删除后重新创建

**包含的岗位类型：**
- 前端开发（React）
- 后端开发（Node.js、Java）
- 全栈开发
- DevOps 工程师
- 数据工程师
- AI/机器学习工程师
- 移动端开发（iOS、Android）
- 产品经理
- UI/UX 设计师
- 安全工程师
- 区块链开发
- 游戏开发
- 运维工程师
- 技术支持
- 架构师
- 数据分析师
- 算法工程师

## 使用流程

### 首次设置

1. 确保数据库已运行并完成迁移：
```bash
npm run db:migrate
```

2. 创建管理员用户：
```bash
npm run db:create-admin
```

3. 初始化岗位数据：
```bash
npm run db:init-jobs
```

### 重新初始化

如果需要重新初始化岗位数据：
```bash
npm run db:init-jobs
# 当提示时输入 'yes' 删除现有数据并重新创建
```

## 注意事项

- 所有脚本都需要在 `server` 目录下运行
- 运行前确保 `.env` 文件配置正确
- 脚本会自动连接到配置的数据库
- 建议在开发环境先测试，确认无误后再在生产环境使用

## 开发新脚本

创建新脚本时，请遵循以下规范：

1. 使用 TypeScript 编写
2. 使用 Prisma Client 操作数据库
3. 添加适当的错误处理
4. 提供清晰的日志输出
5. 在脚本结束时断开数据库连接

**示例模板：**
```typescript
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

async function myScript() {
  try {
    console.log('🚀 Starting...');
    
    // Your code here
    
    console.log('✅ Completed!');
  } catch (error) {
    console.error('❌ Error:', error);
    throw error;
  } finally {
    await prisma.$disconnect();
  }
}

myScript().catch((error) => {
  console.error('Fatal error:', error);
  process.exit(1);
});
```
